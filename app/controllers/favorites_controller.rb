class FavoritesController < ApplicationController

# def create
# Move more or less copies of this to comments controller create
# and favorites controller create. Recipe yummly Id should get passed
# as hidden input in both cases.
#   @yummly = Yummly.find(params[:rid])
#   @recipe = Recipe.where(yummly_id: @yummly.id).
#     first_or_create(description: @yummly.name)
#   redirect_to recipe_path(@recipe)
# end
end
